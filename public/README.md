Dossier public pour les assets statiques.

Usage :
- Images accessibles via `/images/nom.ext` (ex. `/images/avatar.svg`).
- Favicon : `/favicon.svg` (ou `/favicon.ico`).
- OG image : `/og-image.svg` (ou `/og-image.png`).

Remplacez ces placeholders par vos vraies images avant le déploiement.
